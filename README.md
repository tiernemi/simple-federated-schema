This sample project demonstrates how to expose microservices using a federated graphql schema,


To run the services and gateway run the following command
```bash
./gradlew up
```
The graphql playground will be available on
```
http://localhost:4000
```